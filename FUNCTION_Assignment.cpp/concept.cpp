/*#include <iostream>
using namespace std;

// Function to add two integers
int add(int a, int b) {
    return a + b;
}

// Function to add three integers
int add(int a, int b, int c) {
    return a + b + c;
}

int main() {
    int result1 = add(2, 3);
    int result2 = add(2, 3, 5);

    cout << "Result 1: " << result1 << endl;
    cout << "Result 2: " << result2 << endl;

    return 0;
}
*/

/*}
In this example, we have two functions named add, but they take a different number
of parameters. The first add function takes two integers, and the second add function 
takes three integers. The function overloading allows us to use the same name add
for both functions without any conflict. The compiler determines which function 
to call based on the number of arguments passed during the function call.*/